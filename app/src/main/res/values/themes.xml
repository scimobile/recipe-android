<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RecipeAndroid" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!--<item name="colorPrimary">@color/black</item>-->
        <item name="android:includeFontPadding">false</item>
    </style>

    <style name="Theme.RecipeAndroid" parent="Base.Theme.RecipeAndroid" />

    <style name="CustomRatingBarStyle" parent="Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/my_recipe_rating</item>
        <item name="android:indeterminateDrawable">@drawable/my_recipe_rating</item>
        <item name="android:minHeight">15dip</item>
        <item name="android:maxHeight">47dip</item>
    </style>

    <style name="SmallCustomRatingBarStyle" parent="Widget.AppCompat.RatingBar.Small">
        <item name="android:progressDrawable">@drawable/my_recipe_rating</item>
        <item name="android:indeterminateDrawable">@drawable/my_recipe_rating</item>
        <item name="android:minHeight">10dip</item>
        <item name="android:maxHeight">24dip</item>
    </style>

    <style name="ThemeOverlay.App.Toolbar.Surface" parent="ThemeOverlay.Material3.Toolbar.Surface">
        <item name="android:outlineAmbientShadowColor" tools:ignore="NewApi">@android:color/black</item>
        <item name="android:outlineSpotShadowColor" tools:ignore="NewApi">@android:color/black</item>
    </style>

    <style name="ToolBar.TextAppearance" parent="">
        <item name="android:fontFamily">@font/poppins_semibold</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">@color/black</item>
    </style>
    
    <style name="roundedImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">10dp</item>
    </style>

</resources>